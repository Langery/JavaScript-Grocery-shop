## MySQL Note

### 命令操作

```bash
# 启动 MySQL 服务器
sudo mysql.server start

# 进入 MySQL
mysql -u root -p

# 退出 MySQL
exit；

# 停止 MySQL 服务器
sudo mysql.server stop
```

> MySQL 内部语句

``` mysql

```

### Note

1. 在 MySQL 中执行完一条操作后需要加`;`